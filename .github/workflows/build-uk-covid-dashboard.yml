name: build-uk-covid-dashboard
run-name: ${{ github.actor }} is building UK Covid Dashboard
on:
  schedule:
  - cron: "*/10 * * * *"

jobs:
  case-growth-by-region:
    runs-on: ubuntu-latest
    permissions:
      # Give the default GITHUB_TOKEN write permission to commit and push the
      # added or changed files to the repository.
      contents: write
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install datetime
          if [ -f requirements.txts ]; then pip install -r requirements.txt; fi
      - name: Run python script
        run: |
          chmod +x ./UKCovidDashboard/test_python.py
          python ./UKCovidDashboard/test_python.py

      - uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "GH Actions changed files"
          commit_user_name: Laurence Molloy
          commit_user_email: laurence.molloy@gmail.com
          commit_author: Laurence Molloy <laurence.molloy@gmail.com>

#      - name: Upload generated file
#        uses: actions/upload-artifact@v3
#        with:
#          name: out.txt
#          path: .github/artifacts/out.txt

#      - name: commit files  
#        run:
#          git config --local user.name "Laurence Molloy"
#          git config --local user.email "laurence.molloy@gmail.com"
#          git add out.txt
#          git commit -am "GH action files added $(date)"
 #         git push -f origin main

#
#      - name: copy file
#        uses: canastro/copy-file-action@master
#        with:
#          source: "out.txt"
#          target: ".github/artifacts/out.txt"

#      - name: Deploy to main
#        uses: JamesIves/github-pages-deploy-action@4.1.4
#        with:
#          branch: main
#          folder: .github/artifacts